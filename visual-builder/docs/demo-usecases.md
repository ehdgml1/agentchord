# AgentChord Demo Use Cases - 상세 프로세스 명세서

> 5가지 실전 유즈케이스의 블록 조합, 프롬프트, 입출력 데이터 흐름 상세 명세

## 개요 매트릭스

| # | 데모명 | 블록 수 | Trigger | Agent | Multi-Agent | MCP Tool | Feedback | RAG | 전략 | 난이도 |
|---|--------|:------:|:-------:|:-----:|:-----------:|:--------:|:--------:|:---:|------|:------:|
| 1 | PR 코드 리뷰 | 3 | ✓ | ✓ | | ✓×1 | | | - | ★★☆☆☆ |
| 2 | 가격 모니터링 | 3 | ✓ | ✓×2 | | | | | - | ★★★☆☆ |
| 3 | RAG 고객 지원 | 3 | | ✓×2 | | | | ✓ | - | ★★☆☆☆ |
| 4 | 경쟁사 분석 | 6 | ✓ | ✓ | ✓ | ✓×2 | ✓ | | map_reduce | ★★★★☆ |
| 5 | 콘텐츠 마케팅 | 6 | | ✓×2 | ✓ | ✓×2 | ✓ | | debate | ★★★★★ |

**사용 MCP 도구**: GitHub, Puppeteer, Memory, Filesystem, Tavily, Markdown to PDF, Document Converter, Resend, Notion (총 9종, Demo 2·3은 MCP 불필요)

---

## Demo 1: GitHub PR 자동 코드 리뷰

### 개요
- **목적**: 매일 오전 9시 오픈 PR을 자동 리뷰하고 파일로 저장
- **난이도**: ★★☆☆☆ (3노드, 가장 단순)
- **예상 실행 시간**: ~20초
- **예상 비용**: ~$0.002/실행
- **필요 API 키**: `GITHUB_TOKEN`

### 아키텍처

```
┌──────────┐     ┌──────────────────┐     ┌──────────────┐
│ Trigger  │────▶│ Agent            │────▶│ MCP Tool     │
│ 매일 9시  │     │ 코드 리뷰어       │     │ Filesystem   │
│          │     │ + GitHub MCP     │     │ 파일 저장     │
└──────────┘     └──────────────────┘     └──────────────┘
```

### 노드별 상세 명세

#### Node 1: Trigger (매일 오전 9시)

| 항목 | 값 |
|------|-----|
| **타입** | `trigger` |
| **ID** | `trigger-1` |
| **캔버스 위치** | (100, 250) |

```json
{
  "triggerType": "cron",
  "cronExpression": "0 9 * * *"
}
```

**출력**: `{ "timestamp": "2026-02-19T09:00:00Z", "trigger": "cron" }`

---

#### Node 2: Agent (코드 리뷰어)

| 항목 | 값 |
|------|-----|
| **타입** | `agent` |
| **ID** | `agent-reviewer` |
| **모델** | `gemini-2.0-flash` |
| **Temperature** | 0.3 |
| **Max Tokens** | 4096 |
| **MCP Tools** | `["github"]` |
| **캔버스 위치** | (550, 250) |

**System Prompt**:
```
당신은 시니어 소프트웨어 개발자입니다. GitHub 레포지토리의 오픈 PR을 자동으로 리뷰합니다.

## 작업 순서
1. list_pull_requests 도구로 오픈 PR 목록을 가져오세요 (owner: "your-org", repo: "your-repo", state: "open")
2. 각 PR에 대해 get_pull_request 도구로 상세 정보와 변경된 파일을 확인하세요
3. 코드 변경사항을 아래 기준으로 리뷰하세요

## 리뷰 기준
- 보안: SQL injection, XSS, 인증 우회 취약점
- 성능: N+1 쿼리, 불필요한 루프, 메모리 누수
- 클린 코드: 네이밍 컨벤션, 함수 크기, 중복 코드
- 타입 안전성: any 사용, 미처리 에러

## 출력 형식
각 PR별로 다음 형식으로 출력하세요:

### PR #번호: 제목
**심각도**: 🔴 Critical / 🟡 Warning / 🟢 Good
**요약**: 한 줄 요약
**상세 리뷰**:
- 파일명:라인번호 - 이슈 설명
**추천 사항**: 구체적 개선 제안
```

**입력**: Trigger 이벤트 (자동 체이닝). Agent는 GitHub MCP 도구를 사용하여 직접 PR 데이터를 가져옴
**출력**: Markdown 형식의 PR 리뷰 텍스트

---

#### Node 3: MCP Tool (Filesystem 파일 저장)

| 항목 | 값 |
|------|-----|
| **타입** | `mcp_tool` |
| **ID** | `mcp-filesystem` |
| **서버** | Filesystem |
| **도구** | `write_file` |
| **캔버스 위치** | (1000, 250) |

```json
{
  "parameters": {
    "path": "/reviews/pr-review-{{trigger-1.output.timestamp}}.md",
    "content": "{{agent-reviewer.output}}"
  }
}
```

**입력**: `{{agent-reviewer.output}}` - 리뷰 결과 텍스트
**출력**: 파일 저장 경로 확인

### 데이터 흐름 요약

```
trigger-1          → (자동 체이닝) → agent-reviewer
agent-reviewer     → {{agent-reviewer.output}} → mcp-filesystem
```

---

## Demo 2: 경쟁사 가격 모니터링 & 알림

### 개요
- **목적**: 6시간마다 경쟁사 웹사이트의 제품 가격을 스크래핑하고 변동 감지
- **난이도**: ★★★☆☆ (3노드)
- **예상 실행 시간**: ~45-60초
- **예상 비용**: ~$0.003/실행
- **필요 API 키**: 없음 (Puppeteer, Memory는 무료)

### 아키텍처

```
┌──────────┐     ┌──────────────────┐     ┌──────────────────┐
│ Trigger  │────▶│ Agent            │────▶│ Agent            │──▶ Playground 출력
│ 매 6시간  │     │ 가격 수집기       │     │ 비교 분석기       │
│          │     │ + Puppeteer MCP  │     │ + Memory MCP     │
└──────────┘     └──────────────────┘     └──────────────────┘
```

### 노드별 상세 명세

#### Node 1: Trigger (매 6시간)

```json
{
  "triggerType": "cron",
  "cronExpression": "0 */6 * * *"
}
```

---

#### Node 2: Agent (가격 수집기)

| 항목 | 값 |
|------|-----|
| **타입** | `agent` |
| **ID** | `agent-scraper` |
| **모델** | `gemini-2.0-flash` |
| **Temperature** | 0.1 |
| **Max Tokens** | 2048 |
| **MCP Tools** | `["puppeteer"]` |
| **캔버스 위치** | (550, 250) |

**System Prompt**:
```
당신은 웹 스크래핑 전문가입니다. 경쟁사 웹사이트에서 제품 가격 정보를 수집합니다.

## 대상 사이트
1. https://competitor-a.com/products - 메인 상품 3개의 가격
2. https://competitor-b.com/pricing - 요금제별 가격

## 작업 순서
1. puppeteer_navigate 도구로 각 사이트를 방문하세요
2. puppeteer_evaluate 도구로 가격 정보를 추출하세요
   - CSS selector를 사용하여 가격 요소를 찾으세요
   - 예: document.querySelectorAll('.price')
3. 결과를 아래 JSON 형식으로 정리하세요

## 출력 형식 (JSON)
{
  "timestamp": "2026-02-19T15:00:00Z",
  "prices": [
    { "competitor": "A", "product": "상품명", "price": 29900, "currency": "KRW" },
    { "competitor": "A", "product": "상품명2", "price": 59900, "currency": "KRW" },
    { "competitor": "B", "product": "Basic", "price": 9900, "currency": "KRW" },
    { "competitor": "B", "product": "Pro", "price": 29900, "currency": "KRW" }
  ]
}
```

**입력**: Trigger 이벤트 (자동 체이닝)
**출력**: JSON 형식의 가격 데이터

---

#### Node 3: Agent (비교 분석기)

| 항목 | 값 |
|------|-----|
| **타입** | `agent` |
| **ID** | `agent-analyzer` |
| **모델** | `gemini-2.0-flash` |
| **Temperature** | 0.2 |
| **Max Tokens** | 1024 |
| **MCP Tools** | `["memory"]` |
| **캔버스 위치** | (1000, 250) |

**System Prompt**:
```
당신은 가격 분석 전문가입니다. 이전 가격 데이터와 현재 가격을 비교하여 변동을 감지합니다.

## 작업 순서
1. memory_read 도구로 "competitor_prices" 키의 이전 데이터를 읽으세요
2. 입력받은 현재 가격 데이터와 항목별로 비교하세요
3. memory_write 도구로 현재 가격을 "competitor_prices" 키에 저장하세요
4. 5% 이상 변동이 있는 항목이 있으면 알림 메시지를 작성하세요

## 출력 형식
변동 감지 시:
🚨 *경쟁사 가격 변동 감지*
━━━━━━━━━━━━━━━━━━
- [경쟁사A] 상품명: ₩29,900 → ₩27,900 (▼6.7%)
- [경쟁사B] Pro 요금제: ₩29,900 → ₩24,900 (▼16.7%)
━━━━━━━━━━━━━━━━━━
확인 시각: 2026-02-19 15:00 KST

변동 없음:
✅ 경쟁사 가격 변동 없음 (확인: 2026-02-19 15:00 KST)
```

**입력**: `{{agent-scraper.output}}` - 현재 가격 JSON (자동 체이닝)
**출력**: 가격 변동 알림 텍스트 또는 변동 없음 메시지

---

> **Playground 출력**: 이 워크플로우는 Trigger 기반 자동 실행과 Playground 수동 실행 모두 지원합니다. 마지막 노드(`agent-analyzer`)의 출력이 Playground에 표시되며, 스케줄 실행 시에는 실행 로그에서 결과를 확인할 수 있습니다.

### 데이터 흐름 요약

```
trigger-1       → (자동 체이닝) → agent-scraper
agent-scraper   → (자동 체이닝) → agent-analyzer   [JSON 가격 데이터]
agent-analyzer  → Playground/실행 로그에 출력
```

---

## Demo 3: RAG 기반 고객 지원 챗봇

### 개요
- **목적**: 고객 질문을 제품 지식 베이스에서 검색하여 Playground 채팅으로 자동 응답
- **난이도**: ★★☆☆☆ (3노드, RAG 블록 활용, 외부 API 키 불필요)
- **예상 실행 시간**: ~15초
- **예상 비용**: ~$0.002/실행
- **필요 API 키**: 없음 (Gemini API 키만 사전 설정)

### 아키텍처

```
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│ Agent            │────▶│ RAG              │────▶│ Agent            │──▶ Playground 출력
│ 질문 분류기       │     │ 제품 지식 베이스   │     │ 응답 생성기       │
└──────────────────┘     └──────────────────┘     └──────────────────┘
```

### 노드별 상세 명세

#### Node 1: Agent (질문 분류기)

| 항목 | 값 |
|------|-----|
| **타입** | `agent` |
| **ID** | `agent-classifier` |
| **모델** | `gemini-2.0-flash` |
| **Temperature** | 0.1 |
| **Max Tokens** | 512 |
| **캔버스 위치** | (100, 250) |

**Output Fields**:
| 필드명 | 타입 | 설명 |
|--------|------|------|
| `category` | text | FAQ / TECHNICAL / COMPLAINT / GENERAL |
| `query` | text | RAG 검색용 최적화된 질문 |
| `urgency` | text | HIGH / MEDIUM / LOW |

**System Prompt**:
```
당신은 고객 질문 분류 전문가입니다. 고객의 질문을 분석하여 카테고리, 긴급도를 판단하고 RAG 검색에 최적화된 쿼리를 생성합니다.

## 분류 카테고리
- FAQ: 자주 묻는 질문 (가격, 배송, 반품, 결제 등)
- TECHNICAL: 기술 지원 (설치, 오류, 호환성, 설정 등)
- COMPLAINT: 불만/클레임 (품질 불량, 서비스 불만 등)
- GENERAL: 일반 문의

## 긴급도
- HIGH: 서비스 장애, 결제 오류, 개인정보 관련
- MEDIUM: 기능 문의, 설정 도움, 교환/반품
- LOW: 일반 정보 요청, 제안사항

## 쿼리 최적화
원래 질문에서 핵심 키워드를 추출하고, RAG 벡터 검색에 적합한 형태로 재구성하세요.
예) "배송이 왜 이렇게 느려요?" → "배송 지연 소요 기간 안내"
```

**입력**: 고객 질문 텍스트 (워크플로우 시작점, Playground에서 입력)
**출력**: `{ "category": "FAQ", "query": "배송 지연 소요 기간 안내", "urgency": "MEDIUM" }`

---

#### Node 2: RAG (제품 지식 베이스)

| 항목 | 값 |
|------|-----|
| **타입** | `rag` |
| **ID** | `rag-kb` |
| **모델** | `gemini-2.0-flash` |
| **Temperature** | 0.3 |
| **Max Tokens** | 2048 |
| **캔버스 위치** | (550, 250) |

```json
{
  "name": "제품 지식 베이스",
  "documentFiles": [
    { "id": "abc123def456", "filename": "product-manual.md", "size": 45000, "mimeType": "text/markdown" },
    { "id": "ghi789jkl012", "filename": "faq.md", "size": 12000, "mimeType": "text/markdown" },
    { "id": "mno345pqr678", "filename": "troubleshooting.pdf", "size": 230000, "mimeType": "application/pdf" },
    { "id": "stu901vwx234", "filename": "shipping-policy.txt", "size": 8500, "mimeType": "text/plain" }
  ],
  "documents": [],
  "searchLimit": 5,
  "enableBm25": true,
  "chunkSize": 512,
  "chunkOverlap": 50,
  "systemPrompt": "검색된 문서를 기반으로 고객 질문에 정확하게 답변하세요. 문서에 없는 내용은 반드시 '확인 후 안내드리겠습니다'라고 답변하세요. 추측하지 마세요."
}
```

> **문서 소스**: `documentFiles`는 UI에서 드래그앤드롭으로 업로드한 파일 참조입니다 (.txt, .md, .csv, .pdf, .log, .json 지원, 최대 10MB/파일). `documents`는 텍스트 직접 입력용이며, 두 소스 모두 RAG 인덱싱에 사용됩니다. 파일은 `POST /api/documents/upload`로 사전 업로드해야 합니다.

**입력**: `{{agent-classifier.output}}` (자동 체이닝, query 필드가 검색어로 사용됨)
**출력**: 지식 베이스 기반 답변 텍스트

---

#### Node 3: Agent (응답 생성기)

| 항목 | 값 |
|------|-----|
| **타입** | `agent` |
| **ID** | `agent-responder` |
| **모델** | `gemini-2.0-flash` |
| **Temperature** | 0.5 |
| **Max Tokens** | 1024 |
| **캔버스 위치** | (1000, 250) |

**System Prompt**:
```
당신은 친절하고 전문적인 고객 지원 담당자입니다. RAG 검색 결과와 질문 분류 정보를 바탕으로 최종 고객 응답을 작성합니다.

## 규칙
1. 존댓말을 사용하세요
2. 답변은 200자 이내로 간결하게 작성하세요
3. 확실하지 않은 정보는 "담당자에게 연결해 드리겠습니다"를 제안하세요
4. 긴급도가 HIGH인 경우 "즉시 담당자가 확인하겠습니다"를 포함하세요

## 응답 형식
📋 [카테고리] | ⚡ 긴급도: [level]

안녕하세요, 고객님!
(답변 내용)

추가 도움이 필요하시면 말씀해 주세요 😊
```

**입력**: `{{rag-kb.output}}` (자동 체이닝, RAG 응답)
**출력**: 최종 고객 응답 텍스트

---

> **Playground 채팅 출력**: 이 워크플로우는 MCP 도구 없이 동작합니다. 마지막 노드(`agent-responder`)의 출력이 Playground 채팅 인터페이스에 직접 표시됩니다. 멀티턴 대화도 지원되어 고객이 추가 질문을 이어갈 수 있습니다.

### 데이터 흐름 요약

```
(Playground 질문) → agent-classifier   [분류 + 쿼리 최적화]
agent-classifier  → (자동 체이닝) → rag-kb   [최적화된 쿼리로 문서 검색]
rag-kb            → (자동 체이닝) → agent-responder   [RAG 응답 기반 최종 답변]
agent-responder   → Playground 채팅에 출력
```

---

## Demo 4: 주간 경쟁사 분석 보고서

### 개요
- **목적**: 매주 월요일 경쟁사 3곳의 동향을 병렬 리서치하고 Feedback Loop로 품질 보장된 보고서를 PDF+이메일로 발송
- **난이도**: ★★★★☆ (6노드, map_reduce + feedback loop)
- **예상 실행 시간**: ~45-90초
- **예상 비용**: ~$0.012/실행
- **필요 API 키**: `TAVILY_API_KEY`, `RESEND_API_KEY`

### 아키텍처

```
┌──────────┐     ┌──────────────────┐     ┌──────────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ Trigger  │────▶│ Multi-Agent      │────▶│ Agent            │────▶│ Feedback     │────▶│ MCP Tool     │────▶│ MCP Tool     │
│ 매주 월   │     │ 리서치 팀        │     │ 수석 애널리스트   │     │ Loop         │     │ PDF 생성      │     │ 이메일 발송   │
│          │     │ map_reduce       │     │                  │     │ 품질≥8       │     │              │     │              │
└──────────┘     │ ×3 리서처+Tavily │     └──────────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
                 └──────────────────┘              ▲                       │
                                                   └───────────────────────┘
                                                        (품질 미달 시 재실행)
```

### 노드별 상세 명세

#### Node 1: Trigger (매주 월요일 오전 9시)

```json
{
  "triggerType": "cron",
  "cronExpression": "0 9 * * 1"
}
```

---

#### Node 2: Multi-Agent (리서치 팀)

| 항목 | 값 |
|------|-----|
| **타입** | `multi_agent` |
| **ID** | `multi-agent-research` |
| **전략** | `map_reduce` |
| **최대 라운드** | 1 |
| **비용 예산** | $1.0 |
| **캔버스 위치** | (550, 250) |

**멤버 1: 경쟁사A 리서처**

| 항목 | 값 |
|------|-----|
| **이름** | `경쟁사A 리서처` |
| **역할** | `researcher` |
| **모델** | `gemini-2.0-flash` |
| **MCP Tools** | `["web-search"]` |
| **capabilities** | `["research"]` |

```
오늘 날짜는 2026년 2월 19일입니다. 당신은 [경쟁사A] 전문 리서처입니다.

## 조사 항목
1. 최근 1주일 뉴스 및 보도자료 (2026.02.12 ~ 02.19)
2. 신제품/서비스 출시 현황
3. 가격 변동 또는 프로모션
4. 인사/조직 변동
5. 주가/재무 동향

## 규칙
- web_search 도구를 반드시 사용하여 2026년 최신 정보를 검색하세요
- 검색 결과에 없는 내용은 절대 작성하지 마세요
- 출처(URL)를 반드시 명시하세요
- 한국어로 작성하세요

## 출력 형식
### [경쟁사A] 주간 동향 (2026.02.12 ~ 02.19)

#### 주요 뉴스
- [날짜] 뉴스 제목 - 요약 (출처)

#### 시사점
(우리 회사에 미치는 영향 2-3문장)
```

**멤버 2, 3**: 동일 구조, 경쟁사명만 변경

**입력**: Trigger 이벤트 (자동 체이닝)
**출력**: 3명의 리서처 결과를 map_reduce로 합친 종합 리서치

---

#### Node 3: Agent (수석 애널리스트)

| 항목 | 값 |
|------|-----|
| **타입** | `agent` |
| **ID** | `agent-analyst` |
| **모델** | `gemini-2.0-flash` |
| **Temperature** | 0.4 |
| **Max Tokens** | 4096 |
| **캔버스 위치** | (1000, 250) |

**Output Fields**:
| 필드명 | 타입 | 설명 |
|--------|------|------|
| `report` | text | Markdown 형식의 보고서 전문 |
| `quality_score` | number | 자체 품질 평가 점수 (1-10) |

**System Prompt**:
```
당신은 수석 비즈니스 애널리스트입니다. 3명의 리서처가 수집한 경쟁사 정보를 종합 분석하여 **HTML 대시보드 보고서**를 작성합니다.

## 보고서 형식 (HTML + Chart.js)
반드시 아래 구조의 완전한 HTML 문서를 생성하세요. CDN 스크립트를 포함하여 단독 렌더링 가능해야 합니다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>주간 경쟁사 분석 보고서</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    body { font-family: 'Segoe UI', sans-serif; max-width: 1200px; margin: 0 auto; padding: 20px; background: #f8f9fa; }
    .card { background: white; border-radius: 12px; padding: 24px; margin: 16px 0; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
    .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 16px; }
    .metric { text-align: center; padding: 16px; }
    .metric .value { font-size: 2em; font-weight: bold; }
    .metric .label { color: #666; font-size: 0.9em; }
    .threat-high { color: #e74c3c; } .threat-mid { color: #f39c12; } .threat-low { color: #27ae60; }
    table { width: 100%; border-collapse: collapse; }
    th, td { padding: 12px; text-align: left; border-bottom: 1px solid #eee; }
    th { background: #f1f3f5; font-weight: 600; }
    h1 { color: #2c3e50; } h2 { color: #34495e; border-bottom: 2px solid #3498db; padding-bottom: 8px; }
  </style>
</head>
<body>
  <h1>📊 주간 경쟁사 분석 보고서</h1>
  <p>기간: 2026.02.12 ~ 02.19 | 작성: AI 분석팀</p>

  <!-- 핵심 지표 카드 -->
  <div class="grid">
    <div class="card metric">
      <div class="value threat-high">3건</div>
      <div class="label">주요 변화</div>
    </div>
    <!-- 추가 지표 카드들 -->
  </div>

  <!-- Chart.js 차트 -->
  <div class="card">
    <h2>경쟁사 위협도 비교</h2>
    <canvas id="threatChart"></canvas>
  </div>
  <script>
    new Chart(document.getElementById('threatChart'), {
      type: 'radar',
      data: {
        labels: ['제품 혁신', '가격 경쟁력', '마케팅', '기술력', '시장 점유율'],
        datasets: [
          { label: '쿠팡', data: [8,7,9,7,8], borderColor: '#e74c3c', fill: true, backgroundColor: 'rgba(231,76,60,0.1)' },
          { label: '네이버', data: [9,6,8,9,7], borderColor: '#3498db', fill: true, backgroundColor: 'rgba(52,152,219,0.1)' },
          { label: '카카오', data: [7,5,7,8,6], borderColor: '#f1c40f', fill: true, backgroundColor: 'rgba(241,196,15,0.1)' }
        ]
      }
    });
  </script>

  <!-- 위협 & 기회 분석 테이블 -->
  <div class="card">
    <h2>위협 & 기회 분석</h2>
    <table>
      <tr><th>구분</th><th>내용</th><th>영향도</th><th>대응</th></tr>
      <tr><td class="threat-high">⚠️ 위협</td><td>...</td><td>H</td><td>즉시</td></tr>
      <tr><td class="threat-low">✅ 기회</td><td>...</td><td>M</td><td>검토</td></tr>
    </table>
  </div>

  <!-- 경쟁사별 상세 분석 -->
  <div class="card">
    <h2>경쟁사별 상세 분석</h2>
    <!-- 각 경쟁사 섹션 -->
  </div>

  <!-- 권장 조치사항 -->
  <div class="card">
    <h2>권장 조치사항</h2>
    <ol>
      <li><strong>[즉시]</strong> 구체적 액션</li>
      <li><strong>[이번 주]</strong> 구체적 액션</li>
      <li><strong>[모니터링]</strong> 구체적 액션</li>
    </ol>
  </div>
</body>
</html>
```

## 차트 종류 (상황에 맞게 선택)
- radar: 경쟁사 역량 비교
- bar: 수치 비교 (주가, 가격)
- doughnut: 시장 점유율
- line: 트렌드 변화

## 규칙
- 반드시 완전한 HTML 문서를 생성하세요 (<!DOCTYPE html>부터 </html>까지)
- Chart.js CDN을 script 태그로 포함하세요
- 모든 CSS는 인라인 style 태그에 포함하세요
- 데이터는 리서치 결과에 기반하여 실제 수치를 반영하세요
- 한국어로 작성하세요

## 품질 자체 평가
다음 기준으로 1-10점 평가:
- 정보 정확성 (출처 기반 검증)
- 시각화 효과 (차트의 데이터 표현력)
- 분석 깊이 (인사이트 수준)
- 디자인 완성도 (레이아웃, 색상, 가독성)
평균 점수를 quality_score에 기록하세요.
```

**입력**: `{{multi-agent-research.output}}` (자동 체이닝, 종합 리서치 결과)
**출력**: `{ "report": "# 주간 경쟁사...", "quality_score": 7.5 }`

---

#### Node 4: Feedback Loop (품질 검증)

| 항목 | 값 |
|------|-----|
| **타입** | `feedback_loop` |
| **ID** | `feedback-loop` |
| **최대 반복** | 3 |
| **종료 조건** | `quality_score >= 8` |
| **캔버스 위치** | (1450, 250) |

- 종료 조건 미충족 시 → `agent-analyst`로 되돌아가 보고서 개선
- 이전 반복의 출력이 다음 반복의 입력으로 전달됨
- 최대 3회 반복 후 강제 종료

---

#### Node 5: MCP Tool (HTML to PDF)

```json
{
  "serverName": "Document Converter",
  "toolName": "html_to_pdf",
  "parameters": {
    "html": "{{agent-analyst.output.report}}",
    "outputPath": "/reports/weekly_competitor_analysis.pdf"
  }
}
```

---

#### Node 6: MCP Tool (Resend 이메일)

```json
{
  "serverName": "Resend Email",
  "toolName": "send-email",
  "parameters": {
    "to": ["team@company.com"],
    "from": "reports@company.com",
    "subject": "주간 경쟁사 분석 보고서 (2026.02.19)",
    "html": "<h2>주간 경쟁사 분석 보고서</h2><p>첨부 PDF를 확인해주세요.</p>",
    "text": "주간 경쟁사 분석 보고서입니다. 첨부 PDF를 확인해주세요.",
    "attachments": [{
      "filename": "weekly_competitor_analysis.pdf",
      "filePath": "{{mcp-pdf.output}}"
    }]
  }
}
```

### 데이터 흐름 요약

```
trigger           → (자동 체이닝) → multi-agent-research
multi-agent       → (자동 체이닝) → agent-analyst         [종합 리서치 텍스트]
agent-analyst     → (자동 체이닝) → feedback-loop         [{ report, quality_score }]
  └─ quality_score < 8 → agent-analyst로 재실행 (최대 3회)
feedback-loop     → {{agent-analyst.output.report}} → mcp-pdf
mcp-pdf           → {{mcp-pdf.output}} → mcp-email       [PDF 파일 경로]
```

---

## Demo 5: AI 콘텐츠 마케팅 파이프라인

### 개요
- **목적**: 트렌드 리서치 → AI 작가/에디터 토론 → 품질 검증 → PDF + Notion 자동 발행
- **난이도**: ★★★★★ (6노드, debate + feedback loop)
- **예상 실행 시간**: ~60-180초
- **예상 비용**: ~$0.015/실행
- **필요 API 키**: `TAVILY_API_KEY`, `NOTION_API_KEY`

### 아키텍처

```
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ Agent            │────▶│ Multi-Agent      │────▶│ Agent            │────▶│ Feedback     │────▶│ MCP Tool     │────▶│ MCP Tool     │
│ 트렌드 리서처     │     │ 콘텐츠 팀        │     │ 품질 평가기       │     │ Loop         │     │ PDF 생성      │     │ Notion 발행   │
│ + Tavily         │     │ debate           │     │                  │     │ 가독성≥7     │     │              │     │              │
└──────────────────┘     │ 작가 vs 에디터   │     └──────────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
                         └──────────────────┘              ▲                       │
                                                           └───────────────────────┘
                                                                (가독성 미달 시 재토론)
```

### 노드별 상세 명세

#### Node 1: Agent (트렌드 리서처)

| 항목 | 값 |
|------|-----|
| **타입** | `agent` |
| **ID** | `agent-researcher` |
| **모델** | `gemini-2.0-flash` |
| **Temperature** | 0.5 |
| **Max Tokens** | 2048 |
| **MCP Tools** | `["web-search"]` |
| **캔버스 위치** | (100, 250) |

**System Prompt**:
```
당신은 디지털 마케팅 트렌드 리서처입니다. SaaS/B2B 소프트웨어 업계의 최신 트렌드를 조사하여 블로그 콘텐츠 브리프를 작성합니다.

## 작업 순서
1. web_search 도구로 업계 최신 트렌드 키워드를 검색하세요
2. 경쟁사 블로그의 인기 주제를 파악하세요
3. SEO 키워드 트렌드를 확인하세요

## 출력 형식
### 콘텐츠 브리프

**추천 주제**: (구체적 블로그 제목)
**타겟 키워드**: keyword1, keyword2, keyword3
**타겟 독자**: (페르소나 - 직무, 고민, 목표)

**경쟁 콘텐츠 분석**:
- [경쟁사1] "제목" - 강점: / 약점:
- [경쟁사2] "제목" - 강점: / 약점:

**차별화 포인트**: (우리만의 각도와 독창적 관점)

**참고 자료**:
- URL1: 핵심 내용 요약
- URL2: 핵심 내용 요약

**추천 구조**:
1. 도입부 - Hook (독자의 고민 공감)
2. 본론 1 - 현황 분석
3. 본론 2 - 해결 방법/전략
4. 본론 3 - 실전 사례/데이터
5. 결론 - CTA (행동 유도)
```

**입력**: 콘텐츠 주제 또는 키워드 (Playground에서 입력)
**출력**: 콘텐츠 브리프 문서

---

#### Node 2: Multi-Agent (콘텐츠 팀 - debate)

| 항목 | 값 |
|------|-----|
| **타입** | `multi_agent` |
| **ID** | `multi-agent-debate` |
| **전략** | `debate` |
| **최대 라운드** | 3 |
| **비용 예산** | $0.5 |
| **캔버스 위치** | (550, 250) |

**멤버 1: 콘텐츠 라이터**

| 항목 | 값 |
|------|-----|
| **이름** | `콘텐츠 라이터` |
| **역할** | `worker` |
| **모델** | `gemini-2.0-flash` |
| **capabilities** | `["writing", "creativity"]` |

```
당신은 창의적인 B2B 콘텐츠 라이터입니다. 리서치 브리프를 바탕으로 매력적인 블로그 글을 작성합니다.

## 스타일 가이드
- 톤: 전문적이면서도 친근한 대화체
- 문장: 짧고 명확하게 (한 문장 40자 이내 권장)
- 단락: 3-4문장으로 구성
- 소제목: ## H2, ### H3 적절히 활용
- 사례/데이터: 구체적 수치와 실제 사례 포함
- 분량: 1,500-2,000자

## 토론 규칙
- SEO 에디터의 키워드 배치 피드백은 적극 수용하세요
- 단, 가독성을 심각하게 해치는 삽입 요구는 반대 의견과 대안을 제시하세요
- 최종 합의된 버전을 Markdown으로 출력하세요

## 출력
완성된 블로그 글 전문 (Markdown)
```

**멤버 2: SEO 에디터**

| 항목 | 값 |
|------|-----|
| **이름** | `SEO 에디터` |
| **역할** | `worker` |
| **모델** | `gemini-2.0-flash` |
| **capabilities** | `["editing", "seo"]` |

```
당신은 SEO 최적화 전문 에디터입니다. 라이터가 작성한 콘텐츠를 SEO와 품질 관점에서 검수합니다.

## 검수 항목
1. SEO 최적화: 타겟 키워드 자연스러운 배치 (밀도 1-2%)
2. 메타 데이터: 제목 태그(60자 이내), 메타 설명(160자 이내) 작성
3. 구조화: H1→H2→H3 계층 구조, 스캔 가능한 레이아웃
4. 가독성: 짧은 문장, 불릿 포인트, 시각적 구분 요소
5. CTA: 명확하고 구체적인 행동 유도

## 토론 규칙
- 라이터의 가독성 반론이 타당하면 수용하세요
- 키워드 삽입보다 자연스러움을 우선하세요
- 수정 사항은 구체적 위치와 이유를 명시하세요

## 출력
최종 합의된 글 전문 (Markdown) + 메타 데이터:
---
title: "(60자 이내)"
description: "(160자 이내)"
keywords: ["keyword1", "keyword2"]
---
```

**토론 프로세스**:
1. Round 1: 라이터가 브리프 기반 초안 작성 → 에디터가 SEO 관점 피드백
2. Round 2: 라이터가 피드백 반영 수정 → 에디터가 재검수
3. Round 3: 최종 합의 버전 출력

**입력**: `{{agent-researcher.output}}` (자동 체이닝, 콘텐츠 브리프)
**출력**: 토론을 거쳐 합의된 블로그 콘텐츠 (Markdown)

---

#### Node 3: Agent (품질 평가기)

| 항목 | 값 |
|------|-----|
| **타입** | `agent` |
| **ID** | `agent-evaluator` |
| **모델** | `gemini-2.0-flash` |
| **Temperature** | 0.2 |
| **Max Tokens** | 2048 |
| **캔버스 위치** | (1000, 250) |

**Output Fields**:
| 필드명 | 타입 | 설명 |
|--------|------|------|
| `content` | text | 최종 콘텐츠 (수정 없이 그대로 또는 미세 수정) |
| `readability_score` | number | 종합 가독성 점수 (1-10) |

**System Prompt**:
```
당신은 콘텐츠 품질 평가 전문가입니다. 블로그 콘텐츠의 최종 품질을 평가합니다.

## 평가 기준 (각 항목 1-10점)
1. 가독성 (30%): 문장 길이, 구조적 흐름, 스캔 가능성
2. SEO 최적화 (25%): 키워드 배치, 메타 데이터, 구조화
3. 정보 가치 (25%): 독자에게 실질적 도움, 구체적 사례/데이터
4. 매력도 (20%): 제목의 클릭 유도력, 도입부 흡인력, CTA 효과

## 평가 프로세스
1. 각 항목별 점수 산출
2. 가중 평균으로 readability_score 계산 (소수점 1자리)
3. 7점 미만인 경우 구체적 개선 포인트를 content에 코멘트로 추가
4. 7점 이상인 경우 content에 원본을 그대로 출력

## 출력
content 필드에 콘텐츠를, readability_score에 점수를 기록하세요.
```

**입력**: `{{multi-agent-debate.output}}` (자동 체이닝, 토론 결과물)
**출력**: `{ "content": "# 블로그 제목...", "readability_score": 7.8 }`

---

#### Node 4: Feedback Loop (가독성 검증)

| 항목 | 값 |
|------|-----|
| **타입** | `feedback_loop` |
| **ID** | `feedback-loop` |
| **최대 반복** | 3 |
| **종료 조건** | `readability_score >= 7` |
| **캔버스 위치** | (1450, 250) |

- 종료 조건 미충족 시 → `multi-agent-debate`로 되돌아가 재토론
- 평가 코멘트가 다음 라운드의 개선 지침으로 활용됨

---

#### Node 5: MCP Tool (Markdown to PDF)

```json
{
  "serverName": "Markdown to PDF",
  "toolName": "create_pdf_from_markdown",
  "parameters": {
    "markdown": "{{agent-evaluator.output.content}}",
    "filename": "blog_content.pdf"
  }
}
```

---

#### Node 6: MCP Tool (Notion 발행)

```json
{
  "serverName": "Notion",
  "toolName": "create_page",
  "parameters": {
    "database_id": "your-notion-database-id",
    "properties": {
      "Title": "{{agent-researcher.output}}"
    },
    "content": "{{agent-evaluator.output.content}}"
  }
}
```

### 데이터 흐름 요약

```
(주제 입력)       → agent-researcher                    [콘텐츠 브리프]
agent-researcher  → (자동 체이닝) → multi-agent-debate   [브리프 기반 토론]
multi-agent-debate → (자동 체이닝) → agent-evaluator     [토론 결과물 평가]
agent-evaluator   → (자동 체이닝) → feedback-loop        [{ content, readability_score }]
  └─ readability_score < 7 → multi-agent-debate로 재토론 (최대 3회)
feedback-loop     → {{agent-evaluator.output.content}} → mcp-pdf
mcp-pdf           → mcp-notion  (Notion은 {{agent-evaluator.output.content}} 직접 참조)
```

---

## 부록

### A. 템플릿 엔진 문법

| 문법 | 설명 | 예시 |
|------|------|------|
| `{{nodeId.output}}` | 노드의 전체 출력 | `{{agent-reviewer.output}}` |
| `{{nodeId.output.field}}` | 구조화 출력의 특정 필드 | `{{agent-analyst.output.report}}` |
| 자동 체이닝 | edge로 연결된 노드 간 자동 전달 | template 없이 edge만 연결 |

### B. MCP 서버 필요 목록

| MCP 서버 | 사용 데모 | API 키 필요 | 비고 |
|----------|----------|:-----------:|------|
| GitHub | Demo 1 | `GITHUB_TOKEN` | Official |
| Puppeteer | Demo 2 | 불필요 | Official, 무료 |
| Memory | Demo 2 | 불필요 | Official, 무료 |
| Filesystem | Demo 1 | 불필요 | Official, 무료 |
| Web Search (Tavily) | Demo 4, 5 | `TAVILY_API_KEY` | Built-in |
| Markdown to PDF | Demo 5 | 불필요 | 무료 |
| Document Converter | Demo 4 | 불필요 | Puppeteer 기반, Chart.js 렌더링 |
| Resend Email | Demo 4 | `RESEND_API_KEY` | |
| Notion | Demo 5 | `NOTION_API_KEY` | |

### C. 모델 추천

| 용도 | 추천 모델 | 이유 |
|------|----------|------|
| 일반 에이전트 | `gemini-2.0-flash` | 빠르고 저렴 ($0.10/1M input) |
| 고품질 분석 | `gpt-4o` | 더 정확한 분석이 필요할 때 |
| 비용 절약 | `gpt-4o-mini` | 간단한 분류/판단 작업 |

### C-1. 문서 업로드 API

RAG 노드에서 사용할 문서 파일을 사전 업로드하는 API입니다.

| 엔드포인트 | 메서드 | Rate Limit | 설명 |
|-----------|--------|:----------:|------|
| `/api/documents/upload` | POST | 10/min | 파일 업로드 (multipart/form-data) |
| `/api/documents` | GET | 60/min | 업로드된 파일 목록 조회 |
| `/api/documents/{file_id}` | DELETE | 30/min | 파일 삭제 |

**지원 포맷**: `.txt`, `.md`, `.csv`, `.pdf`, `.log`, `.json`
**제한**: 단일 파일 10MB, 사용자당 총 50MB
**인증**: JWT Bearer 토큰 필수
**보안**: 확장자 화이트리스트, PDF 매직바이트 검증, 경로 순회 방어

### D. 비용 요약

| 데모 | 실행당 비용 | 월간 비용 (스케줄 기준) |
|------|:----------:|:---------------------:|
| 1. PR 코드 리뷰 | ~$0.002 | ~$0.06 (매일) |
| 2. 가격 모니터링 | ~$0.003 | ~$0.36 (6시간마다, MCP 불필요) |
| 3. 고객 지원 | ~$0.002 | 사용량 비례 (MCP 불필요) |
| 4. 경쟁사 분석 | ~$0.012 | ~$0.05 (매주) |
| 5. 콘텐츠 마케팅 | ~$0.015 | 사용량 비례 |

---

## E. 구현 가이드

### E.1 워크플로우 JSON 포맷

```
참고 파일: /Users/ud/Documents/work/news-workflow-import.json (뉴스 브리핑 데모, 실행 검증 완료)
```

워크플로우는 다음 JSON 구조를 따릅니다:

```json
{
  "version": "1.0",
  "exportedAt": "ISO8601 timestamp",
  "workflow": {
    "name": "워크플로우 이름",
    "description": "설명",
    "nodes": [
      {
        "id": "unique-node-id",
        "type": "agent | multi_agent | mcp_tool | trigger | feedback_loop | rag",
        "position": { "x": number, "y": number },
        "data": { /* BlockData - 타입별 상이 */ }
      }
    ],
    "edges": [
      { "id": "e1", "source": "source-node-id", "target": "target-node-id" }
    ]
  }
}
```

### E.2 블록 타입별 data 필드 매핑

#### trigger

| 필드 | 타입 | 필수 | 설명 |
|------|------|:---:|------|
| triggerType | "cron" \| "webhook" | ✓ | 트리거 유형 |
| cronExpression | string | | cron일 때 필수 (예: "0 9 * * *") |
| webhookPath | string | | webhook일 때 필수 |

#### agent (AgentBlockData)

| 필드 | 타입 | 필수 | 설명 |
|------|------|:---:|------|
| label | string | ✓ | 캔버스에 표시되는 이름 |
| name | string | ✓ | 에이전트 이름 |
| role | string | ✓ | 역할 설명 |
| model | ModelId | ✓ | LLM 모델 (추천: "gemini-2.0-flash") |
| systemPrompt | string | ✓ | 시스템 프롬프트 |
| temperature | number | ✓ | 0~1 (분석: 0.1-0.3, 창작: 0.5-0.7) |
| maxTokens | number | ✓ | 최대 토큰 (512~4096) |
| mcpTools | string[] | | MCP 도구 ID 배열 (예: ["web-search", "github"]) |
| outputFields | OutputFieldConfig[] | | 구조화 출력 필드 정의 |

#### multi_agent (MultiAgentBlockData)

| 필드 | 타입 | 필수 | 설명 |
|------|------|:---:|------|
| label | string | ✓ | 캔버스에 표시되는 이름 |
| strategy | string | ✓ | "coordinator" \| "round_robin" \| "debate" \| "map_reduce" |
| maxRounds | number | ✓ | 최대 라운드 수 |
| costBudget | number | ✓ | 비용 예산 (USD) |
| enableConsult | boolean | | 피어 상담 활성화 |
| members | AgentMemberConfig[] | ✓ | 팀원 목록 |

**members[] 각 항목** (AgentMemberConfig):

| 필드 | 타입 | 필수 | 설명 |
|------|------|:---:|------|
| name | string | ✓ | 팀원 이름 |
| role | string | ✓ | "coordinator" \| "worker" \| "reviewer" \| "specialist" |
| model | string | ✓ | LLM 모델 |
| systemPrompt | string | ✓ | 시스템 프롬프트 |
| capabilities | string[] | ✓ | 능력 태그 (예: ["research", "writing"]) |
| mcpTools | string[] | | MCP 도구 ID 배열 |

#### mcp_tool (MCPToolBlockData)

| 필드 | 타입 | 필수 | 설명 |
|------|------|:---:|------|
| label | string | ✓ | 캔버스에 표시되는 이름 |
| serverId | string | ✓ | MCP 서버 UUID (동적, 빈 문자열 가능) |
| serverName | string | ✓ | MCP 서버 이름 (매칭에 사용) |
| toolName | string | ✓ | 도구 이름 |
| description | string | ✓ | 도구 설명 |
| parameters | object | ✓ | 도구별 파라미터 (템플릿 사용 가능) |

#### feedback_loop (FeedbackLoopBlockData)

| 필드 | 타입 | 필수 | 설명 |
|------|------|:---:|------|
| maxIterations | number | ✓ | 최대 반복 횟수 (추천: 3) |
| stopCondition | string | ✓ | 종료 조건식 (예: "quality_score >= 8") |

#### rag (RAGBlockData)

| 필드 | 타입 | 필수 | 설명 |
|------|------|:---:|------|
| name | string | ✓ | RAG 노드 이름 |
| documentFiles | DocumentFileInfo[] | | 업로드된 파일 참조 (UI 드래그앤드롭) |
| documents | string[] | | 텍스트 직접 입력 (인라인) |
| searchLimit | number | ✓ | 검색 결과 수 (추천: 5) |
| enableBm25 | boolean | ✓ | BM25 키워드 검색 활성화 |
| chunkSize | number | ✓ | 청크 크기 (추천: 512) |
| chunkOverlap | number | ✓ | 청크 오버랩 (추천: 50) |
| systemPrompt | string | ✓ | RAG 응답 생성 프롬프트 |
| model | string | ✓ | LLM 모델 |
| temperature | number | ✓ | 온도 |
| maxTokens | number | ✓ | 최대 토큰 |
| embeddingProvider | string | | 임베딩 제공자 ("openai" \| "gemini" \| "ollama") |
| embeddingModel | string | | 임베딩 모델 이름 |
| embeddingDimensions | number | | 임베딩 차원 수 |

> **문서 입력 방식**: `documentFiles`와 `documents` 중 하나 이상 필수. 두 소스 모두 사용 가능하며 합쳐서 인덱싱됩니다. `documentFiles`의 각 항목은 `{ id, filename, size, mimeType }` 형태이며, `POST /api/documents/upload`로 사전 업로드 필요합니다. 지원 포맷: .txt, .md, .csv, .pdf, .log, .json (최대 10MB/파일, 사용자당 총 50MB).

### E.3 데이터 흐름 규칙

워크플로우에서 데이터는 세 가지 메커니즘으로 전달됩니다:

#### 1. 자동 체이닝 (Auto Chaining)

edge로 연결된 노드 간 자동으로 upstream 출력이 downstream 입력으로 전달됩니다. 별도 설정 불필요하며 executor의 `_resolve_input()`이 처리합니다.

#### 2. 템플릿 참조

- `{{nodeId.output}}`: 전체 출력 참조
- `{{nodeId.output.field}}`: 구조화 출력의 특정 필드 참조
- MCP Tool의 parameters에서 주로 사용

#### 3. OutputFields 구조화 출력

Agent에 outputFields를 정의하면 JSON 형태로 출력됩니다. Feedback Loop의 stopCondition에서 필드 참조 가능합니다.

#### 중요 규칙

- 자동 체이닝과 템플릿 참조는 동시에 작동합니다
- Feedback Loop의 stopCondition은 직전 노드의 outputFields 필드명을 사용합니다
- MCP Tool의 serverId는 동적 UUID로 매번 변경됨 → 빈 문자열("")로 두고 serverName으로 매칭합니다

### E.4 구현 현황

| 데모 | 상태 | 비고 |
|------|:----:|------|
| 뉴스 브리핑 (기존) | ✅ 완료 | `news-workflow-import.json`, 실행 검증 완료 (34.4s, 2452 tokens) |
| Demo 1: PR 코드 리뷰 | ❌ 미구현 | JSON 생성 필요 |
| Demo 2: 가격 모니터링 | ❌ 미구현 | JSON 생성 필요 |
| Demo 3: RAG 고객 지원 | ❌ 미구현 | JSON 생성 필요 |
| Demo 4: 경쟁사 분석 | ❌ 미구현 | JSON 생성 필요 |
| Demo 5: 콘텐츠 마케팅 | ❌ 미구현 | JSON 생성 필요 |

### E.5 실행 시 주의사항

#### 1. serverId는 동적

MCP 서버의 UUID는 백엔드 재시작마다 변경됩니다. JSON에서 `serverId: ""` (빈 문자열)로 두고, `serverName`으로 서버 식별합니다. Import 시 UI에서 수동 매핑 또는 executor가 serverName으로 자동 매칭합니다.

#### 2. MCP 서버 사전 설치 필수

워크플로우 실행 전 필요한 MCP 서버가 모두 설치되어 있어야 합니다. MCP Marketplace에서 설치하며, API 키 필요한 서버는 키 입력 후 Install 합니다.

#### 3. Builtin 도구 (web-search)

`mcpTools: ["web-search"]`는 MCP 서버가 아닌 내장 Tavily 웹 검색입니다. `backend/.env`에 `TAVILY_API_KEY` 설정 필요합니다.

#### 4. Gemini 도구 호출 특성

Gemini 모델은 도구 사용 시 시스템 프롬프트에 명시적 지시가 필요합니다. executor가 자동으로 "You MUST use these tools..." 힌트를 주입하지만, 프롬프트에도 "반드시 web_search 도구를 사용하세요" 같은 지시를 포함하면 더 안정적입니다.

#### 5. Synthesis Round

도구 호출 후 빈 응답 방지를 위해 executor가 자동으로 synthesis round (도구 없는 최종 LLM 호출)를 수행합니다. 별도 설정 불필요합니다.

#### 6. Feedback Loop 연결

feedback_loop 노드의 `loopBodyStart`는 adjacency에서 자동 감지됩니다. edge로 feedback_loop → 재실행 대상 노드를 연결하면 됩니다.

#### 7. Import 방법

UI의 ImportDialog를 통해 JSON 파일을 import하거나, `/api/workflows` POST로 직접 생성할 수 있습니다.

#### 8. Rate Limiting

Auth 엔드포인트 5/min, Execution 10/min으로 제한됩니다. 데모 연속 실행 시 주의하세요.
