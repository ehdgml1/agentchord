# L5: Hide Nginx version in response headers
server_tokens off;

upstream backend {
    server backend:8000;
}

# HTTP server - Development/non-TLS mode
server {
    listen 80;
    server_name _;

    # Security headers
    # CSP managed by backend middleware - do not duplicate here
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options DENY always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Request size limit
    client_max_body_size 10m;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
    gzip_min_length 256;

    # Frontend static files
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # API proxy
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_read_timeout 120s;
        proxy_send_timeout 60s;
    }

    # WebSocket proxy
    location /ws/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Health check endpoint
    location /health/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
    }
}

# HTTPS server - Production TLS mode (requires cert volume mount)
# Uncomment and configure when deploying with TLS certificates
# server {
#     listen 443 ssl http2;
#     server_name _;
#
#     # TLS Configuration
#     ssl_certificate /etc/nginx/certs/cert.pem;
#     ssl_certificate_key /etc/nginx/certs/key.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
#     ssl_prefer_server_ciphers on;
#
#     # Security headers
#     # CSP managed by backend middleware - do not duplicate here
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Content-Type-Options nosniff always;
#     add_header X-Frame-Options DENY always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#     # Request size limit
#     client_max_body_size 10m;
#
#     # Gzip compression
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml;
#     gzip_min_length 256;
#
#     # Frontend static files
#     location / {
#         root /usr/share/nginx/html;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     # API proxy
#     location /api/ {
#         proxy_pass http://backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         # Timeouts
#         proxy_connect_timeout 60s;
#         proxy_read_timeout 120s;
#         proxy_send_timeout 60s;
#     }
#
#     # WebSocket proxy
#     location /ws/ {
#         proxy_pass http://backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
#
#     # Health check endpoint
#     location /health/ {
#         proxy_pass http://backend;
#         proxy_set_header Host $host;
#     }
# }
#
# # HTTP to HTTPS redirect - Production mode
# # Uncomment when enabling HTTPS
# server {
#     listen 80;
#     server_name _;
#     return 301 https://$host$request_uri;
# }
